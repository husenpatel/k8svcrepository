apiVersion: v1
kind: Service
metadata:
  name: vertex-svcb
  labels:
    app: myvertexappb
spec:
  type: NodePort
  ports:
  - port: 8080
    nodePort: 30002
    protocol: TCP
  selector:
    app: myvertexappb