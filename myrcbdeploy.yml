apiVersion: apps/v1
kind: Deployment
metadata:
  name: vertex-deploy
  labels:
    app: vertexappb
spec:
  replicas: 3
  selector:
      matchLabels:
         app: vertexapp
  template:
     metadata:
       labels: 
         app: vertexapp
     spec:
       containers:
        - name: myvertexapp-container
          image:  husenpatel/warbbimage20323:1
          ports:
            - containerPort: 8080
